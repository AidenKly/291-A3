# Q3: Rewrite query Q2 but not using any VIEW.

# (Q2: Create a VIEW called OrderSize which has two columns, oid and size, where oid is an order_id
# and size is the total number of items in that order. Using the view OrderSize, extend Q1 with the
# orders that have items more than the average number of items in the orders. )

USE MOST OF KAYE'S CODE